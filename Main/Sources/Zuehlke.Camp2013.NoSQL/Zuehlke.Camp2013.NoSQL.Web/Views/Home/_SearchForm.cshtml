@model Zuehlke.Camp2013.NoSQL.Shared.Models.SearchParameter

@using (Html.BeginForm("Search", "Home", FormMethod.Get))
		{
		    <h3>Zuehlke Search</h3>
            @Html.TextBoxFor(m => m.Query, new { Id = "SearchQuery" })

			<input type="submit" value="Search" />
		}